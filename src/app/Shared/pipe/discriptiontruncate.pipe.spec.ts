import { DiscriptiontruncatePipe } from './discriptiontruncate.pipe';

describe('DiscriptiontruncatePipe', () => {
  it('create an instance', () => {
    const pipe = new DiscriptiontruncatePipe();
    expect(pipe).toBeTruthy();
  });
});
